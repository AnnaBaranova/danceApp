<%- include('../partials/header') %>


    <div class="container">
        <form action="/events" id="new-form" method="POST">
            <label>Title:</label>
            <input placeholder="don't start with space :)" type="text" name="title" required pattern="\w+.*">
            <label>Details:</label>
            <input type="text" name="details">
            <label>Limit of Guests:<span> "at least 2 people"</span></label>
            <input type="number" name="guestLimit" value="6">
            <label>Date:<span> "Choose at least 1 day from now and not more than 90 days"</span></label>
            <input min="<%= new Date(new Date().setDate(new Date().getDate() + 1)).toISOString().slice(0, 16)%>"
            max="<%= new Date(new Date().setMonth(new Date().getMonth() + 3)).toISOString().slice(0, 16)%>"
             type="datetime-local" name="date" value="">
            <label>Place:</label>
            <input type="text" name="place">
            <button class="btn-floating btn-large waves-effect waves-light red" type="submit"><i
                    class="material-icons">send</i></button>
        </form>
    </div>


    <%- include('../partials/footer') %>

